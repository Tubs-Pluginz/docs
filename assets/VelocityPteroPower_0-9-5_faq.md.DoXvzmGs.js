import{_ as t,c as o,o as i,ag as s}from"./chunks/framework.BDwTZuFy.js";const h=JSON.parse('{"title":"VPP faq","description":"","frontmatter":{"title":"VPP faq"},"headers":[],"relativePath":"VelocityPteroPower/0-9-5/faq.md","filePath":"VelocityPteroPower/0-9-5/faq.md"}'),r={name:"VelocityPteroPower/0-9-5/faq.md"};function l(a,e,n,c,d,u){return i(),o("div",null,e[0]||(e[0]=[s('<h1 id="frequently-asked-questions-faq-❓" tabindex="-1">Frequently Asked Questions (FAQ) ❓ <a class="header-anchor" href="#frequently-asked-questions-faq-❓" aria-label="Permalink to &quot;Frequently Asked Questions (FAQ) ❓&quot;">​</a></h1><p>Here are answers to common questions and issues encountered with VelocityPteroPower.</p><div class="tip custom-block"><p class="custom-block-title">Before You Ask</p><p>Many common issues can be resolved by:</p><ol><li>Ensuring you have the <strong>latest version</strong> of VelocityPteroPower.</li><li>Carefully <strong>checking your <code>config.yml</code></strong> for typos or misconfigurations.</li><li>Enabling <strong>debug logging</strong> (<code>loggerLevel: 10</code> in <code>config.yml</code>) and reviewing the console output. Refer to the <a href="./troubleshooting.html">Troubleshooting Guide</a> for more detailed steps.</li></ol></div><h2 id="general-startup" tabindex="-1">General &amp; Startup <a class="header-anchor" href="#general-startup" aria-label="Permalink to &quot;General &amp; Startup&quot;">​</a></h2><details><summary><b>Q: My server isn&#39;t starting automatically when a player tries to connect. What should I check?</b></summary><ul><li><strong>VPP Configuration (<code>config.yml</code>):</strong><ul><li>Is the server name under <code>servers:</code> an <strong>exact match</strong> (case-sensitive) to the name in your <code>velocity.toml</code>?</li><li>Is the <code>id:</code> for that server the correct <strong>short alphanumeric ID</strong> from your panel (e.g., <code>a1b2c3d4</code>), not a long UUID?</li></ul></li><li><strong>Panel API Connection (<code>pterodactyl:</code> section in <code>config.yml</code>):</strong><ul><li>Is <code>url:</code> the full, correct URL to your panel (e.g., <code>https://panel.example.com/</code>)?</li><li>Is <code>apiKey:</code> a <strong>Client API Key</strong> (starts <code>ptlc_</code> or <code>plcn_</code>)? Application API Keys (<code>ptla_</code>, <code>peli_</code>) are <strong>not supported</strong>.</li><li>Does the API key have necessary permissions on the panel (power control, file read for whitelist)?</li></ul></li><li><strong>Velocity Console Logs:</strong><ul><li>Enable debug logging (<code>loggerLevel: 10</code>).</li><li>Look for errors from <code>[VPP]</code> on startup (e.g., &quot;Invalid API Key&quot;) or when a player attempts connection.</li></ul></li><li><strong>Panel API Rate Limits:</strong><ul><li>Set <code>printRateLimit: true</code> in <code>config.yml</code>.</li><li>Are you seeing &quot;Rate limit reached&quot; messages?</li></ul></li><li><strong><code>serverStatusCheckMethod</code>:</strong><ul><li>If <code>VELOCITY_PING</code>: Is the backend server&#39;s IP/port in <code>velocity.toml</code> correct and reachable for pings from the proxy? Is <code>pingTimeout</code> adequate?</li><li>If <code>PANEL_API</code>: This relies on the panel API being responsive.</li></ul></li><li><strong>Whitelist Feature:</strong><ul><li>If <code>whitelist: true</code> for the server: Is the player on the server&#39;s <code>whitelist.json</code>? Or do they have <code>ptero.bypass</code> permission (and <code>whitelistAllowBypass: true</code> is set)?</li><li>Remember, whitelist fetching is not supported for MC Server Soft.</li></ul></li></ul></details><details><summary><b>Q: What&#39;s the difference between `timeout` (per-server) and `idleStartShutdownTime` (global)?</b></summary><ul><li><strong><code>timeout</code>:</strong> This setting is configured <em>per-server</em> in the <code>servers:</code> section. It applies when an <strong>active server (that had players)</strong> becomes empty. The countdown for shutdown begins after the last player leaves.</li><li><strong><code>idleStartShutdownTime</code>:</strong> This is a <em>global</em> setting. It applies if a server is <strong>started by VPP</strong> (e.g., due to a player connection attempt or a command) but <strong>no player successfully joins it</strong> within this specified duration. It&#39;s a safeguard to shut down servers that were initiated but never actually used.</li></ul></details><details><summary><b>Q: Why does it sometimes take a moment for me to connect to a server after VPP starts it?</b></summary> This is likely due to the `startupJoinDelay` setting, configured per-server in `config.yml`. This delay is intentionally added *after* VPP detects the server as &quot;online&quot; (via `serverStatusCheckMethod`). It gives the backend Minecraft server and its plugins additional time to fully initialize and load before VPP attempts to transfer the player. Without this, players might be connected too early, before the server is truly ready to accept them. </details><details><summary><b>Q: Is <code>idleShutdownCheckInterval</code> in minutes or seconds?</b></summary><ul><li><strong>Seconds.</strong> The automatic idle shutdown sweep uses seconds for its interval.</li><li>In code, it is scheduled with <code>TimeUnit.SECONDS</code>, so set <code>idleShutdownCheckInterval: 300</code> for 5 minutes, for example.</li><li>Set to <code>0</code> or a negative value to disable the sweep.</li></ul></details><h2 id="shutdown-issues" tabindex="-1">Shutdown Issues <a class="header-anchor" href="#shutdown-issues" aria-label="Permalink to &quot;Shutdown Issues&quot;">​</a></h2><details><summary><b>Q: Servers aren&#39;t stopping automatically when they become empty.</b></summary><ul><li><strong><code>timeout</code> Setting:</strong> For the specific server in <code>config.yml</code>, is <code>timeout:</code> set to a positive number (seconds)? <ul><li>If <code>timeout: -1</code>, automatic shutdown for that server is disabled.</li><li>If <code>timeout: 0</code>, it should attempt to stop immediately (though panel processing takes time).</li></ul></li><li><strong>Velocity Console Logs:</strong> Enable debug logging. Look for messages like &quot;Scheduling server shutdown for...&quot;, &quot;Shutdown cancelled for server...&quot;, or &quot;Failed to shutdown server...&quot;.</li><li><strong>Player Count Accuracy:</strong> Is Velocity accurately reporting the server as empty? Rarely, a player might be &quot;ghosted&quot; on the proxy.</li><li><strong>Panel API Rate Limits:</strong> If VPP is rate-limited, it cannot send the stop signal. Check with <code>printRateLimit: true</code>.</li><li><strong>Panel/Server Issues:</strong><ul><li>Is the panel API responsive?</li><li>Check the console of the backend Minecraft server on the panel itself. Is it stuck or refusing to shut down cleanly?</li></ul></li><li><strong><code>shutdownRetries</code> &amp; <code>shutdownRetryDelay</code>:</strong> VPP will attempt to confirm shutdown and retry. If these retries fail, it will log an error.</li></ul></details><h2 id="limbo" tabindex="-1">Limbo <a class="header-anchor" href="#limbo" aria-label="Permalink to &quot;Limbo&quot;">​</a></h2><details><summary><b>Q: The limbo feature isn&#39;t working as expected.</b></summary><ul><li><strong>Configuration (<code>config.yml</code>):</strong><ul><li>Ensure at least one valid limbo is listed under <code>lobbyBalancer.limbos</code> and registered in your <code>velocity.toml</code>.</li></ul></li><li><strong>Limbo Status:</strong><ul><li>Is a limbo server actually online and accessible via Velocity?</li><li>If a limbo is <em>also</em> managed by VPP, ensure its own <code>timeout</code> and startup settings in <code>config.yml</code> are appropriate (e.g., <code>-1</code> to keep it online).</li></ul></li><li><strong>Velocity Console Logs:</strong> Look for messages from VPP about &quot;Redirecting player to limbo...&quot; or any errors related to finding or connecting to a limbo server.</li></ul></details><h2 id="lobbies-limbo" tabindex="-1">Lobbies &amp; Limbo <a class="header-anchor" href="#lobbies-limbo" aria-label="Permalink to &quot;Lobbies &amp; Limbo&quot;">​</a></h2><details><summary><b>Q: Can I use more than one lobby or limbo?</b></summary> Yes. Add your Velocity server names under <code>lobbyBalancer.lobbies</code> and/or <code>lobbyBalancer.limbos</code> in <code>config.yml</code>. The plugin will pick one automatically based on the strategy you choose. </details><details><summary><b>Q: Do lobbies need to be in the top-level <code>servers:</code> list?</b></summary> Only if you want VPP to auto-start them. If a lobby is not in <code>servers:</code>, VPP can still use it while it is already online, but won’t be able to start it. </details><details><summary><b>Q: Which strategy should I pick?</b></summary> Start with <b>ROUND_ROBIN</b>. If player counts are uneven, try <b>LEAST_PLAYERS</b>. If you want the lowest-CPU lobby (and you use the panel resource endpoint), use <b>LEAST_CPU</b>. </details><details><summary><b>Q: What happens if all lobbies are down?</b></summary> VPP will try your <code>lobbyBalancer.limbos</code> list. If none are usable, players may be disconnected or kept on their current server until the target starts. </details><details><summary><b>Q: How does auto-start work for lobbies?</b></summary> If <code>autoScaleEnabled: true</code> and your lobbies are listed under <code>servers:</code> with valid panel IDs, VPP can keep <code>minOnline</code> lobbies running and start more when players fill up existing ones (<code>playersPerServer</code>) or, for <b>LEAST_CPU</b>, when CPU usage is above <code>cpuScaleUpThreshold</code>. </details><h2 id="api-whitelist" tabindex="-1">API &amp; Whitelist <a class="header-anchor" href="#api-whitelist" aria-label="Permalink to &quot;API &amp; Whitelist&quot;">​</a></h2><details><summary><b>Q: I&#39;m getting &quot;API rate limit exceeded&quot; messages in the console.</b></summary><ul><li><strong>Enable <code>printRateLimit: true</code></strong> in <code>config.yml</code>. This will show you the rate limit values VPP receives from your panel, helping confirm if this is the issue.</li><li><strong>Other API Consumers:</strong> Are other plugins, scripts, or services also making frequent requests to your panel API? This can contribute to hitting the limit.</li><li><strong><code>apiThreads</code>:</strong> The default (<code>10</code>) is usually fine. Excessively high values here could potentially hit rate limits faster if many operations are triggered at once.</li><li><strong>Panel Documentation:</strong> Consult your panel&#39;s documentation to understand its specific API rate limits and how they are applied.</li></ul></details><details><summary><b>Q: The whitelist feature isn&#39;t working, or `/ptero whitelistReload` does nothing/shows errors.</b></summary><ul><li><strong>Server Configuration (<code>config.yml</code>):</strong> Is <code>whitelist: true</code> set for the specific server(s) in the <code>servers:</code> section?</li><li><div class="danger custom-block"><p class="custom-block-title">MC Server Soft Incompatibility</p><p>The whitelist fetching feature is <strong>NOT SUPPORTED</strong> if VPP detects your panel as <strong>MC Server Soft</strong>. This is due to differences in panel APIs for file access. VPP will log a warning, and whitelist checks will be skipped.</p></div></li><li><strong>Panel API Key Permissions:</strong> Your Client API Key (<code>ptlc_</code> or <code>plcn_</code>) must have permissions on the panel to <strong>read files</strong> for the target server (specifically, to access <code>whitelist.json</code>).</li><li><strong>File Existence &amp; Location:</strong> Does <code>whitelist.json</code> actually exist in the <strong>root directory</strong> of the Minecraft server on the panel?</li><li><strong>Velocity Console Logs (Debug Enabled):</strong> Look for messages like: <ul><li>&quot;Fetching whitelist for server...&quot;</li><li>&quot;Updated whitelist for server...&quot;</li><li>Errors such as &quot;Failed to fetch whitelist for server...&quot; or &quot;Error parsing whitelist JSON...&quot;</li></ul></li><li><strong><code>whitelistCheckInterval</code>:</strong> If you expect periodic updates, ensure this is set to a positive value (minutes).</li></ul></details><details><summary><b>Q: Why does the memory limit sometimes show as &quot;unlimited&quot; in /ptero info?</b></summary><ul><li>The value comes from your panel&#39;s reported limit: <ul><li><strong>Pterodactyl:</strong> <code>memory_limit_bytes</code> from <code>/resources</code> may be missing or <code>0</code>; we then fall back to <code>limits.memory</code> (MiB) from <code>/servers/{id}</code>. A value of <code>0</code> means unlimited.</li><li><strong>Pelican:</strong> Uses <code>attributes.limits.memory</code> (MiB). <code>0</code> means unlimited.</li><li><strong>MC Server Soft:</strong> Uses <code>latest.memoryLimit</code> (MiB). <code>0</code> means unlimited.</li></ul></li><li>When the limit is <code>0</code> (unlimited), the plugin displays &quot;unlimited&quot; for memory. Set a concrete memory cap in your panel if you want a finite limit shown.</li></ul></details><details><summary><b>Q: Can VPP manage game servers hosted on different Pterodactyl/Pelican Nodes (Wings/Daemon machines)? What about different Panel installations?</b></summary><ul><li><p><strong>Multiple Nodes (Wings/Daemon) under a Single Panel: YES</strong> VelocityPteroPower connects to one specific Panel installation (defined by the pterodactyl.url in config.yml). This single Panel installation can (and typically does) manage game servers running on many different physical or virtual machines, which are known as Nodes (running the Pterodactyl Wings or Pelican Daemon software). VPP can manage any game server that is registered under that one Panel, regardless of which Node it&#39;s hosted on.</p></li><li><p><strong>Multiple, Separate Panel Installations: NO (with a single VPP instance)</strong> A single instance of VelocityPteroPower is designed to communicate with only one Panel installation at a time. If you have completely separate Pterodactyl or Pelican panel installations (e.g., panel1.example.com and panel2.example.com), you cannot manage servers from both of these distinct panels with a single VPP plugin instance. To manage servers across entirely separate panel installations, you would theoretically need to run separate, isolated Velocity proxy instances, each with its own VPP plugin configured to point to its respective panel. This is generally not a common or straightforward setup.</p></li></ul></details><hr><p>If your question isn&#39;t answered here, please check the <a href="./troubleshooting.html">Troubleshooting Guide</a> or join our <a href="https://discord.pluginz.dev" target="_blank" rel="noreferrer">Discord Community</a> for further assistance!</p>',25)]))}const g=t(r,[["render",l]]);export{h as __pageData,g as default};
