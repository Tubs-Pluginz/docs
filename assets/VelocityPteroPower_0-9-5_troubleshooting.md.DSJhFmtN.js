import{_ as o,c as t,o as s,ag as i}from"./chunks/framework.BDwTZuFy.js";const h=JSON.parse('{"title":"VPP troubleshooting","description":"","frontmatter":{"title":"VPP troubleshooting"},"headers":[],"relativePath":"VelocityPteroPower/0-9-5/troubleshooting.md","filePath":"VelocityPteroPower/0-9-5/troubleshooting.md"}'),r={name:"VelocityPteroPower/0-9-5/troubleshooting.md"};function l(n,e,a,c,d,u){return s(),t("div",null,e[0]||(e[0]=[i('<h1 id="troubleshooting-guide-🛠️" tabindex="-1">Troubleshooting Guide 🛠️ <a class="header-anchor" href="#troubleshooting-guide-🛠️" aria-label="Permalink to &quot;Troubleshooting Guide 🛠️&quot;">​</a></h1><p>This guide provides steps to help you diagnose and resolve common issues with VelocityPteroPower (VPP).</p><h2 id="_1-preliminary-checks-✅" tabindex="-1">1. Preliminary Checks ✅ <a class="header-anchor" href="#_1-preliminary-checks-✅" aria-label="Permalink to &quot;1. Preliminary Checks ✅&quot;">​</a></h2><ul><li><strong>Latest VPP Version:</strong> Ensure you are running the latest stable release of VelocityPteroPower. Check the <a href="https://modrinth.com/plugin/velocitypteropower" target="_blank" rel="noreferrer">Modrinth Page</a> or use the in-plugin update checker (if <code>checkUpdate: true</code> in <code>config.yml</code>).</li><li><strong>Velocity Version:</strong> Confirm your Velocity proxy version is compatible. VPP is generally built against recent Velocity API versions.</li></ul><h2 id="_2-enable-debug-logging-📝" tabindex="-1">2. Enable Debug Logging 📝 <a class="header-anchor" href="#_2-enable-debug-logging-📝" aria-label="Permalink to &quot;2. Enable Debug Logging 📝&quot;">​</a></h2><p>This is the <strong>most crucial step</strong> for effective troubleshooting.</p><ol><li>Open <code>plugins/VelocityPteroPower/config.yml</code>.</li><li>Locate the <code>loggerLevel</code> setting.</li><li>Change its value to <code>10</code>:<div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">loggerLevel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # From 20 (Info) to 10 (Debug)</span></span></code></pre></div></li><li>Save the <code>config.yml</code> file.</li><li>Execute <code>/ptero reload</code> in the Velocity console or restart the Velocity proxy.</li></ol><p>Now, reproduce the issue you are experiencing. Carefully examine the Velocity console logs for messages prefixed with <code>[VPP]</code> or <code>VelocityPteroPower</code>, as well as any related stack traces or error messages.</p><div class="tip custom-block"><p class="custom-block-title">Remember to Revert!</p><p>Once troubleshooting is complete, remember to set <code>loggerLevel</code> back to <code>20</code> (Info) for normal operation to prevent excessive console spam.</p></div><h2 id="_3-common-issues-solutions" tabindex="-1">3. Common Issues &amp; Solutions <a class="header-anchor" href="#_3-common-issues-solutions" aria-label="Permalink to &quot;3. Common Issues &amp; Solutions&quot;">​</a></h2><h3 id="a-server-not-starting-stopping-as-expected" tabindex="-1">A. Server Not Starting/Stopping as Expected <a class="header-anchor" href="#a-server-not-starting-stopping-as-expected" aria-label="Permalink to &quot;A. Server Not Starting/Stopping as Expected&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>Players attempt to connect to an offline server, but it doesn&#39;t start.</li><li>An empty server remains online despite <code>timeout</code> settings.</li><li>Commands like <code>/ptero start</code> or <code>/ptero stop</code> seem to have no effect.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong>Configuration Accuracy (<code>config.yml</code>):</strong><ul><li><strong><code>servers.&lt;serverName&gt;</code>:</strong> Does the server name (e.g., <code>hub</code>) <strong>exactly match</strong> the name defined in your <code>velocity.toml</code>? (This is case-sensitive).</li><li><strong><code>servers.&lt;serverName&gt;.id</code>:</strong> Is this the correct <strong>short alphanumeric ID</strong> from your panel for that specific server (e.g., <code>a1b2c3d4</code> from the panel URL), not a long Docker container ID or full UUID?</li><li><strong><code>pterodactyl.url</code>:</strong> Is this the full, correct base URL to your panel, including <code>http://</code> or <code>https://</code> (e.g., <code>https://panel.example.com/</code>)?</li><li><strong><code>pterodactyl.apiKey</code>:</strong><ul><li>Is it a <strong>Client API Key</strong> (starts <code>ptlc_</code> for Pterodactyl or <code>plcn_</code> for Pelican)?</li><li><strong>Application API Keys (<code>ptla_</code>, <code>peli_</code>) are NOT supported.</strong></li><li>Does the key have the necessary permissions on the panel (e.g., control power signals, read server files if using the whitelist feature)?</li></ul></li></ul></li><li><strong>Panel API Connectivity:</strong><ul><li>From the machine running your Velocity proxy, can you access the panel URL in a browser or via <code>curl</code>?</li><li>Check VPP&#39;s startup logs (with debug enabled) for messages like &quot;Successfully connected to panel&quot; or errors like &quot;Invalid API Key,&quot; &quot;Failed to initialize Panel API Client,&quot; or connection timeouts.</li></ul></li><li><strong>API Rate Limits:</strong><ul><li>Set <code>printRateLimit: true</code> in <code>config.yml</code>.</li><li>Look for console messages from VPP indicating &quot;Rate limit reached&quot; or &quot;API request blocked due to rate limiting.&quot; If you see these, your panel is temporarily throttling requests from VPP.</li></ul></li><li><strong><code>serverStatusCheckMethod</code> (<code>config.yml</code>):</strong><ul><li>If <code>VELOCITY_PING</code>: Is the backend server&#39;s IP address and port correctly configured in <code>velocity.toml</code>? Is the server network-reachable from the proxy machine? Is the <code>pingTimeout</code> value in <code>config.yml</code> sufficient?</li><li>If <code>PANEL_API</code>: This method relies entirely on the panel API being responsive and accurate.</li></ul></li><li><strong>Whitelist Feature (for server starting issues):</strong><ul><li>If <code>whitelist: true</code> is set for the server in <code>config.yml</code>: <ul><li>Is the connecting player listed in that server&#39;s <code>whitelist.json</code> file on the panel?</li><li>If <code>whitelistAllowBypass: true</code> is also set, does the player have the <code>ptero.bypass</code> permission?</li><li>Remember: Whitelist fetching is <strong>not supported for MC Server Soft panels.</strong></li></ul></li></ul></li><li><strong><code>timeout</code> / <code>idleStartShutdownTime</code> (for server stopping issues):</strong><ul><li>For per-server auto-shutdown: Is <code>timeout:</code> in <code>servers.&lt;serverName&gt;:</code> set to a positive number (seconds)? A value of <code>-1</code> disables it.</li><li>For servers started but never joined: Is <code>idleStartShutdownTime:</code> (global) set to a positive number? <code>-1</code> disables it.</li></ul></li><li><strong>Backend Server Logs (Panel Console):</strong><ul><li>Always check the console output of the actual Minecraft server on the Pterodactyl/Pelican panel. Are there errors there preventing it from starting up correctly or shutting down cleanly? VPP can only send signals; it can&#39;t fix underlying issues with the Minecraft server itself.</li></ul></li></ol></details><h3 id="b-limbo-server-functionality-issues" tabindex="-1">B. Limbo Server Functionality Issues <a class="header-anchor" href="#b-limbo-server-functionality-issues" aria-label="Permalink to &quot;B. Limbo Server Functionality Issues&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>Players are disconnected instead of being sent to the limbo server.</li><li>Players are sent to limbo but never transferred to the target server.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong><code>limboServer</code> Configuration (<code>config.yml</code>):</strong><ul><li>Is the server name specified in <code>limboServer:</code> an <strong>exact match</strong> (case-sensitive) for a server defined in your <code>velocity.toml</code>?</li><li>Is it still set to the default <code>&quot;changeMe&quot;</code>?</li></ul></li><li><strong>Limbo Server Accessibility:</strong><ul><li>Is the server designated as your limbo server actually online and joinable directly via Velocity (e.g., using <code>/server limboServerName</code>)?</li><li>If your limbo server is <em>also</em> managed by VPP, check its own configuration within the <code>servers:</code> section of <code>config.yml</code>. Ensure its <code>timeout</code> is set appropriately (e.g., <code>-1</code> to prevent it from auto-shutting down, or a very short <code>timeout</code> if it&#39;s designed to be ephemeral but also quick to start via VPP).</li></ul></li><li><strong>VPP Logs (Debug Enabled):</strong><ul><li>Look for messages like &quot;Redirecting player to limbo server...&quot; or any errors related to finding, pinging, or connecting to the limbo server.</li><li>Check for messages about the target server&#39;s startup progress when a player is in limbo.</li></ul></li></ol></details><h3 id="c-whitelist-feature-not-working-correctly" tabindex="-1">C. Whitelist Feature Not Working Correctly <a class="header-anchor" href="#c-whitelist-feature-not-working-correctly" aria-label="Permalink to &quot;C. Whitelist Feature Not Working Correctly&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>Players who should be whitelisted are denied by VPP (or vice-versa).</li><li>The <code>/ptero whitelistReload</code> command shows errors or doesn&#39;t seem to update.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong><code>whitelist: true</code>:</strong> Is this set for the specific server(s) in the <code>servers:</code> section of <code>config.yml</code>?</li><li><div class="danger custom-block"><p class="custom-block-title">MC Server Soft Incompatibility</p><p>The panel API functionality required for VPP to fetch <code>whitelist.json</code> is <strong>not available on MC Server Soft panels.</strong> If VPP detects this panel type, the whitelist feature will be effectively disabled, and warnings may be logged.</p></div></li><li><strong>Panel API Key Permissions:</strong> Your Client API Key (<code>ptlc_</code> or <code>plcn_</code>) <strong>must</strong> have permissions on the panel to <strong>read server files</strong> (specifically, to access <code>whitelist.json</code> for the relevant servers).</li><li><strong>File Path &amp; Name:</strong> VPP expects the whitelist file to be named exactly <code>whitelist.json</code> and located in the <strong>root directory</strong> of the Minecraft server&#39;s files on the panel.</li><li><strong><code>whitelist.json</code> Format:</strong> Is the <code>whitelist.json</code> file on the panel correctly formatted JSON? (e.g., <code>[{&quot;uuid&quot;: &quot;...&quot;, &quot;name&quot;: &quot;...&quot;}, ...]</code>)</li><li><strong>VPP Logs (Debug Enabled):</strong> Look for: <ul><li>&quot;Fetching whitelist for server <code>serverName</code>...&quot;</li><li>&quot;Updated whitelist for server <code>serverName</code>: <code>X</code> players&quot;</li><li>Error messages like &quot;Failed to fetch whitelist for server...&quot;, &quot;Error parsing whitelist JSON...&quot;, or &quot;Whitelist file not found for server...&quot;.</li></ul></li><li><strong><code>whitelistCheckInterval</code>:</strong> If you expect periodic automatic updates, ensure this is set to a positive value (in minutes).</li></ol></details><h3 id="d-http-2-goaway-errors-in-logs" tabindex="-1">D. HTTP/2 &quot;GOAWAY&quot; Errors in Logs <a class="header-anchor" href="#d-http-2-goaway-errors-in-logs" aria-label="Permalink to &quot;D. HTTP/2 &quot;GOAWAY&quot; Errors in Logs&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Explanation</summary><p><strong>Symptoms:</strong></p><ul><li>Debug logs show messages containing &quot;GOAWAY&quot; during API requests to the panel.</li></ul><p><strong>Explanation:</strong> This typically indicates a temporary issue with the HTTP/2 connection between your Velocity server and the panel. It can be caused by the panel itself, network intermediaries (like reverse proxies or Cloudflare), or transient network conditions. VPP includes some internal retry logic for these specific errors.</p><p><strong>Solutions:</strong></p><ul><li>Often, these are transient and may resolve themselves.</li><li>If persistent: <ul><li>Check the health and logs of your Pterodactyl/Pelican panel.</li><li>Investigate the network path between your Velocity server and the panel.</li><li>If using Cloudflare or another reverse proxy in front of your panel, check its configuration and logs for any HTTP/2 related issues or connection resets.</li></ul></li></ul></details><h3 id="e-plugin-fails-to-load-or-throws-errors-on-velocity-startup" tabindex="-1">E. Plugin Fails to Load or Throws Errors on Velocity Startup <a class="header-anchor" href="#e-plugin-fails-to-load-or-throws-errors-on-velocity-startup" aria-label="Permalink to &quot;E. Plugin Fails to Load or Throws Errors on Velocity Startup&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>VelocityPteroPower does not appear in <code>/velocity plugins</code>.</li><li>Errors related to <code>de.tubyoub.velocitypteropower</code> appear in the console during Velocity&#39;s startup sequence.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong>Java Version:</strong> VelocityPteroPower requires <strong>Java 21 or higher</strong>. Ensure your Velocity proxy is running on a compatible Java version. Use <code>java -version</code> in your server&#39;s terminal.</li><li><strong>Corrupted JAR File:</strong> The downloaded <code>VelocityPteroPower.jar</code> might be corrupted. Try re-downloading it from the official source (Modrinth/GitHub Releases).</li><li><strong>Incomplete Upload:</strong> Ensure the JAR file was fully uploaded to your server&#39;s <code>plugins</code> directory.</li><li><strong>File Permissions:</strong> Unlikely, but ensure the Velocity process has read access to the JAR file.</li><li><strong><code>config.yml</code> Syntax Errors (Severe Cases):</strong> While VPP uses BoostedYAML which is robust, a severely malformed <code>config.yml</code> (e.g., completely broken YAML syntax) <em>could</em> theoretically cause loading issues. If you suspect this, back up your current <code>config.yml</code>, delete it from the <code>plugins/VelocityPteroPower/</code> folder, and let VPP regenerate a default one on the next startup. Then, carefully re-apply your settings.</li><li><strong>Conflicting Plugins (Very Rare):</strong> In extremely rare cases, another Velocity plugin might cause a severe classpath conflict if it bundles incompatible versions of shared libraries that VPP also uses (though VPP shades its dependencies like BoostedYAML and Jackson to minimize this). If VPP only fails to load when certain other plugins are present, this might be a factor to investigate.</li></ol></details><h2 id="_4-providing-information-for-support-🆘" tabindex="-1">4. Providing Information for Support 🆘 <a class="header-anchor" href="#_4-providing-information-for-support-🆘" aria-label="Permalink to &quot;4. Providing Information for Support 🆘&quot;">​</a></h2><p>If you&#39;ve gone through these steps and are still stuck, please seek help! The best place is the <a href="https://discord.pluginz.dev" target="_blank" rel="noreferrer">Support Discord Server</a>.</p><p>When asking for support, please provide as much of the following information as possible to help us diagnose the issue quickly and effectively:</p><ul><li><strong>VelocityPteroPower Version:</strong> (e.g., <code>0.9.4</code>)</li><li><strong>Velocity Version:</strong> (e.g., <code>3.3.0-SNAPSHOT (git-...)</code> - get from <code>/velocity version</code>)</li><li><strong>Panel Type &amp; Version:</strong> (e.g., Pterodactyl 1.11.5, Pelican 0.8.2)</li><li><strong>Java Version:</strong></li><li><strong>Your <code>config.yml</code>:</strong><div class="danger custom-block"><p class="custom-block-title">Mask API Key!</p><p>Before sharing your <code>config.yml</code>, <strong>ALWAYS MASK YOUR <code>apiKey</code></strong> like this: <code>apiKey: &quot;ptlc_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX&quot;</code></p></div><ul><li><strong>Preferred Sharing Method:</strong> Please upload your <code>config.yml</code> (with masked API key) to <a href="https://pastebin.com/" target="_blank" rel="noreferrer"><strong>Pastebin</strong></a> and provide the link.</li></ul></li><li><strong>Relevant language file(s)</strong> from <code>plugins/VelocityPteroPower/messages/&lt;lang&gt;.yml</code> (if you&#39;ve customized them and it relates to the issue). You can also use <a href="https://pastebin.com/" target="_blank" rel="noreferrer">Pastebin</a> for this.</li><li><strong>Detailed Console Logs (with Debug Enabled):</strong><ul><li>Ensure <code>loggerLevel: 10</code> is set in your <code>config.yml</code>.</li><li>Capture logs from the moment Velocity starts up through the point where the issue occurs.</li><li><strong>Preferred Sharing Method:</strong> Please upload your full console logs to <a href="https://mclo.gs/" target="_blank" rel="noreferrer"><strong>mclo.gs</strong></a> and provide the link. This site is specifically designed for Minecraft logs and offers helpful analysis.</li></ul><div class="tip custom-block"><p class="custom-block-title">Do Not Paste Directly</p><p>Please <strong>do not</strong> paste long logs or configuration files directly into Discord channels. Use the recommended services above and share the links.</p></div></li><li><strong>A Clear Description of the Issue:</strong><ul><li>What did you expect to happen?</li><li>What actually happened?</li><li>Were th--- title: VPP troubleshooting</li></ul></li></ul><hr><h1 id="troubleshooting-guide-🛠️-1" tabindex="-1">Troubleshooting Guide 🛠️ <a class="header-anchor" href="#troubleshooting-guide-🛠️-1" aria-label="Permalink to &quot;Troubleshooting Guide 🛠️&quot;">​</a></h1><p>This guide provides steps to help you diagnose and resolve common issues with VelocityPteroPower (VPP).</p><h2 id="_1-preliminary-checks-✅-1" tabindex="-1">1. Preliminary Checks ✅ <a class="header-anchor" href="#_1-preliminary-checks-✅-1" aria-label="Permalink to &quot;1. Preliminary Checks ✅&quot;">​</a></h2><ul><li><strong>Latest VPP Version:</strong> Ensure you are running the latest stable release of VelocityPteroPower. Check the <a href="https://modrinth.com/plugin/velocitypteropower" target="_blank" rel="noreferrer">Modrinth Page</a> or use the in-plugin update checker (if <code>checkUpdate: true</code> in <code>config.yml</code>).</li><li><strong>Velocity Version:</strong> Confirm your Velocity proxy version is compatible. VPP is generally built against recent Velocity API versions.</li></ul><h2 id="_2-enable-debug-logging-📝-1" tabindex="-1">2. Enable Debug Logging 📝 <a class="header-anchor" href="#_2-enable-debug-logging-📝-1" aria-label="Permalink to &quot;2. Enable Debug Logging 📝&quot;">​</a></h2><p>This is the <strong>most crucial step</strong> for effective troubleshooting.</p><ol><li>Open <code>plugins/VelocityPteroPower/config.yml</code>.</li><li>Locate the <code>loggerLevel</code> setting.</li><li>Change its value to <code>10</code>:<div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">loggerLevel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # From 20 (Info) to 10 (Debug)</span></span></code></pre></div></li><li>Save the <code>config.yml</code> file.</li><li>Execute <code>/ptero reload</code> in the Velocity console or restart the Velocity proxy.</li></ol><p>Now, reproduce the issue you are experiencing. Carefully examine the Velocity console logs for messages prefixed with <code>[VPP]</code> or <code>VelocityPteroPower</code>, as well as any related stack traces or error messages.</p><div class="tip custom-block"><p class="custom-block-title">Remember to Revert!</p><p>Once troubleshooting is complete, remember to set <code>loggerLevel</code> back to <code>20</code> (Info) for normal operation to prevent excessive console spam.</p></div><h2 id="_3-common-issues-solutions-1" tabindex="-1">3. Common Issues &amp; Solutions <a class="header-anchor" href="#_3-common-issues-solutions-1" aria-label="Permalink to &quot;3. Common Issues &amp; Solutions&quot;">​</a></h2><h3 id="a-server-not-starting-stopping-as-expected-1" tabindex="-1">A. Server Not Starting/Stopping as Expected <a class="header-anchor" href="#a-server-not-starting-stopping-as-expected-1" aria-label="Permalink to &quot;A. Server Not Starting/Stopping as Expected&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>Players attempt to connect to an offline server, but it doesn&#39;t start.</li><li>An empty server remains online despite <code>timeout</code> settings.</li><li>Commands like <code>/ptero start</code> or <code>/ptero stop</code> seem to have no effect.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong>Configuration Accuracy (<code>config.yml</code>):</strong><ul><li><strong><code>servers.&lt;serverName&gt;</code>:</strong> Does the server name (e.g., <code>hub</code>) <strong>exactly match</strong> the name defined in your <code>velocity.toml</code>? (This is case-sensitive).</li><li><strong><code>servers.&lt;serverName&gt;.id</code>:</strong> Is this the correct <strong>short alphanumeric ID</strong> from your panel for that specific server (e.g., <code>a1b2c3d4</code> from the panel URL), not a long Docker container ID or full UUID?</li><li><strong><code>pterodactyl.url</code>:</strong> Is this the full, correct base URL to your panel, including <code>http://</code> or <code>https://</code> (e.g., <code>https://panel.example.com/</code>)?</li><li><strong><code>pterodactyl.apiKey</code>:</strong><ul><li>Is it a <strong>Client API Key</strong> (starts <code>ptlc_</code> for Pterodactyl or <code>plcn_</code> for Pelican)?</li><li><strong>Application API Keys (<code>ptla_</code>, <code>peli_</code>) are NOT supported.</strong></li><li>Does the key have the necessary permissions on the panel (e.g., control power signals, read server files if using the whitelist feature)?</li></ul></li></ul></li><li><strong>Panel API Connectivity:</strong><ul><li>From the machine running your Velocity proxy, can you access the panel URL in a browser or via <code>curl</code>?</li><li>Check VPP&#39;s startup logs (with debug enabled) for messages like &quot;Successfully connected to panel&quot; or errors like &quot;Invalid API Key,&quot; &quot;Failed to initialize Panel API Client,&quot; or connection timeouts.</li></ul></li><li><strong>API Rate Limits:</strong><ul><li>Set <code>printRateLimit: true</code> in <code>config.yml</code>.</li><li>Look for console messages from VPP indicating &quot;Rate limit reached&quot; or &quot;API request blocked due to rate limiting.&quot; If you see these, your panel is temporarily throttling requests from VPP.</li></ul></li><li><strong><code>serverStatusCheckMethod</code> (<code>config.yml</code>):</strong><ul><li>If <code>VELOCITY_PING</code>: Is the backend server&#39;s IP address and port correctly configured in <code>velocity.toml</code>? Is the server network-reachable from the proxy machine? Is the <code>pingTimeout</code> value in <code>config.yml</code> sufficient?</li><li>If <code>PANEL_API</code>: This method relies entirely on the panel API being responsive and accurate.</li></ul></li><li><strong>Whitelist Feature (for server starting issues):</strong><ul><li>If <code>whitelist: true</code> is set for the server in <code>config.yml</code>: <ul><li>Is the connecting player listed in that server&#39;s <code>whitelist.json</code> file on the panel?</li><li>If <code>whitelistAllowBypass: true</code> is also set, does the player have the <code>ptero.bypass</code> permission?</li><li>Remember: Whitelist fetching is <strong>not supported for MC Server Soft panels.</strong></li></ul></li></ul></li><li><strong><code>timeout</code> / <code>idleStartShutdownTime</code> (for server stopping issues):</strong><ul><li>For per-server auto-shutdown: Is <code>timeout:</code> in <code>servers.&lt;serverName&gt;:</code> set to a positive number (seconds)? A value of <code>-1</code> disables it.</li><li>For servers started but never joined: Is <code>idleStartShutdownTime:</code> (global) set to a positive number? <code>-1</code> disables it.</li></ul></li><li><strong>Backend Server Logs (Panel Console):</strong><ul><li>Always check the console output of the actual Minecraft server on the Pterodactyl/Pelican panel. Are there errors there preventing it from starting up correctly or shutting down cleanly? VPP can only send signals; it can&#39;t fix underlying issues with the Minecraft server itself.</li></ul></li></ol></details><h3 id="b-limbo-server-functionality-issues-1" tabindex="-1">B. Limbo Server Functionality Issues <a class="header-anchor" href="#b-limbo-server-functionality-issues-1" aria-label="Permalink to &quot;B. Limbo Server Functionality Issues&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>Players are disconnected instead of being sent to the limbo server.</li><li>Players are sent to limbo but never transferred to the target server.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong>Limbo configuration (<code>config.yml</code>):</strong><ul><li>Ensure at least one valid limbo is listed under <code>lobbyBalancer.limbos</code> and registered in your <code>velocity.toml</code>.</li></ul></li><li><strong>Limbo Accessibility:</strong><ul><li>Is a limbo server actually online and joinable directly via Velocity (e.g., using <code>/server &lt;limboServerName&gt;</code>)?</li><li>If a limbo is <em>also</em> managed by VPP, check its own configuration within the <code>servers:</code> section of <code>config.yml</code>. Ensure its <code>timeout</code> is set appropriately (e.g., <code>-1</code> to prevent it from auto-shutting down, or a very short <code>timeout</code> if it&#39;s designed to be ephemeral but also quick to start via VPP).</li></ul></li><li><strong>VPP Logs (Debug Enabled):</strong><ul><li>Look for messages like &quot;Redirecting player to limbo server...&quot; or any errors related to finding, pinging, or connecting to the limbo server.</li><li>Check for messages about the target server&#39;s startup progress when a player is in limbo.</li></ul></li></ol></details><h3 id="c-whitelist-feature-not-working-correctly-1" tabindex="-1">C. Whitelist Feature Not Working Correctly <a class="header-anchor" href="#c-whitelist-feature-not-working-correctly-1" aria-label="Permalink to &quot;C. Whitelist Feature Not Working Correctly&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>Players who should be whitelisted are denied by VPP (or vice-versa).</li><li>The <code>/ptero whitelistReload</code> command shows errors or doesn&#39;t seem to update.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong><code>whitelist: true</code>:</strong> Is this set for the specific server(s) in the <code>servers:</code> section of <code>config.yml</code>?</li><li><div class="danger custom-block"><p class="custom-block-title">MC Server Soft Incompatibility</p><p>The panel API functionality required for VPP to fetch <code>whitelist.json</code> is <strong>not available on MC Server Soft panels.</strong> If VPP detects this panel type, the whitelist feature will be effectively disabled, and warnings may be logged.</p></div></li><li><strong>Panel API Key Permissions:</strong> Your Client API Key (<code>ptlc_</code> or <code>plcn_</code>) <strong>must</strong> have permissions on the panel to <strong>read server files</strong> (specifically, to access <code>whitelist.json</code> for the relevant servers).</li><li><strong>File Path &amp; Name:</strong> VPP expects the whitelist file to be named exactly <code>whitelist.json</code> and located in the <strong>root directory</strong> of the Minecraft server&#39;s files on the panel.</li><li><strong><code>whitelist.json</code> Format:</strong> Is the <code>whitelist.json</code> file on the panel correctly formatted JSON? (e.g., <code>[{&quot;uuid&quot;: &quot;...&quot;, &quot;name&quot;: &quot;...&quot;}, ...]</code>)</li><li><strong>VPP Logs (Debug Enabled):</strong> Look for: <ul><li>&quot;Fetching whitelist for server <code>serverName</code>...&quot;</li><li>&quot;Updated whitelist for server <code>serverName</code>: <code>X</code> players&quot;</li><li>Error messages like &quot;Failed to fetch whitelist for server...&quot;, &quot;Error parsing whitelist JSON...&quot;, or &quot;Whitelist file not found for server...&quot;.</li></ul></li><li><strong><code>whitelistCheckInterval</code>:</strong> If you expect periodic automatic updates, ensure this is set to a positive value (in minutes).</li></ol></details><h3 id="d-http-2-goaway-errors-in-logs-1" tabindex="-1">D. HTTP/2 &quot;GOAWAY&quot; Errors in Logs <a class="header-anchor" href="#d-http-2-goaway-errors-in-logs-1" aria-label="Permalink to &quot;D. HTTP/2 &quot;GOAWAY&quot; Errors in Logs&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Explanation</summary><p><strong>Symptoms:</strong></p><ul><li>Debug logs show messages containing &quot;GOAWAY&quot; during API requests to the panel.</li></ul><p><strong>Explanation:</strong> This typically indicates a temporary issue with the HTTP/2 connection between your Velocity server and the panel. It can be caused by the panel itself, network intermediaries (like reverse proxies or Cloudflare), or transient network conditions. VPP includes some internal retry logic for these specific errors.</p><p><strong>Solutions:</strong></p><ul><li>Often, these are transient and may resolve themselves.</li><li>If persistent: <ul><li>Check the health and logs of your Pterodactyl/Pelican panel.</li><li>Investigate the network path between your Velocity server and the panel.</li><li>If using Cloudflare or another reverse proxy in front of your panel, check its configuration and logs for any HTTP/2 related issues or connection resets.</li></ul></li></ul></details><h3 id="e-plugin-fails-to-load-or-throws-errors-on-velocity-startup-1" tabindex="-1">E. Plugin Fails to Load or Throws Errors on Velocity Startup <a class="header-anchor" href="#e-plugin-fails-to-load-or-throws-errors-on-velocity-startup-1" aria-label="Permalink to &quot;E. Plugin Fails to Load or Throws Errors on Velocity Startup&quot;">​</a></h3><details class="details custom-block"><summary>Symptoms &amp; Checklist</summary><p><strong>Symptoms:</strong></p><ul><li>VelocityPteroPower does not appear in <code>/velocity plugins</code>.</li><li>Errors related to <code>de.tubyoub.velocitypteropower</code> appear in the console during Velocity&#39;s startup sequence.</li></ul><p><strong>Checklist:</strong></p><ol><li><strong>Java Version:</strong> VelocityPteroPower requires <strong>Java 21 or higher</strong>. Ensure your Velocity proxy is running on a compatible Java version. Use <code>java -version</code> in your server&#39;s terminal.</li><li><strong>Corrupted JAR File:</strong> The downloaded <code>VelocityPteroPower.jar</code> might be corrupted. Try re-downloading it from the official source (Modrinth/GitHub Releases).</li><li><strong>Incomplete Upload:</strong> Ensure the JAR file was fully uploaded to your server&#39;s <code>plugins</code> directory.</li><li><strong>File Permissions:</strong> Unlikely, but ensure the Velocity process has read access to the JAR file.</li><li><strong><code>config.yml</code> Syntax Errors (Severe Cases):</strong> While VPP uses BoostedYAML which is robust, a severely malformed <code>config.yml</code> (e.g., completely broken YAML syntax) <em>could</em> theoretically cause loading issues. If you suspect this, back up your current <code>config.yml</code>, delete it from the <code>plugins/VelocityPteroPower/</code> folder, and let VPP regenerate a default one on the next startup. Then, carefully re-apply your settings.</li><li><strong>Conflicting Plugins (Very Rare):</strong> In extremely rare cases, another Velocity plugin might cause a severe classpath conflict if it bundles incompatible versions of shared libraries that VPP also uses (though VPP shades its dependencies like BoostedYAML and Jackson to minimize this). If VPP only fails to load when certain other plugins are present, this might be a factor to investigate.</li></ol></details><h2 id="_4-providing-information-for-support-🆘-1" tabindex="-1">4. Providing Information for Support 🆘 <a class="header-anchor" href="#_4-providing-information-for-support-🆘-1" aria-label="Permalink to &quot;4. Providing Information for Support 🆘&quot;">​</a></h2><p>If you&#39;ve gone through these steps and are still stuck, please seek help! The best place is the <a href="https://discord.pluginz.dev" target="_blank" rel="noreferrer">Support Discord Server</a>.</p><p>When asking for support, please provide as much of the following information as possible to help us diagnose the issue quickly and effectively:</p><ul><li><strong>VelocityPteroPower Version:</strong> (e.g., <code>0.9.4</code>)</li><li><strong>Velocity Version:</strong> (e.g., <code>3.3.0-SNAPSHOT (git-...)</code> - get from <code>/velocity version</code>)</li><li><strong>Panel Type &amp; Version:</strong> (e.g., Pterodactyl 1.11.5, Pelican 0.8.2)</li><li><strong>Java Version:</strong></li><li><strong>Your <code>config.yml</code>:</strong><div class="danger custom-block"><p class="custom-block-title">Mask API Key!</p><p>Before sharing your <code>config.yml</code>, <strong>ALWAYS MASK YOUR <code>apiKey</code></strong> like this: <code>apiKey: &quot;ptlc_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX&quot;</code></p></div><ul><li><strong>Preferred Sharing Method:</strong> Please upload your <code>config.yml</code> (with masked API key) to <a href="https://pastebin.com/" target="_blank" rel="noreferrer"><strong>Pastebin</strong></a> and provide the link.</li></ul></li><li><strong>Relevant language file(s)</strong> from <code>plugins/VelocityPteroPower/messages/&lt;lang&gt;.yml</code> (if you&#39;ve customized them and it relates to the issue). You can also use <a href="https://pastebin.com/" target="_blank" rel="noreferrer">Pastebin</a> for this.</li><li><strong>Detailed Console Logs (with Debug Enabled):</strong><ul><li>Ensure <code>loggerLevel: 10</code> is set in your <code>config.yml</code>.</li><li>Capture logs from the moment Velocity starts up through the point where the issue occurs.</li><li><strong>Preferred Sharing Method:</strong> Please upload your full console logs to <a href="https://mclo.gs/" target="_blank" rel="noreferrer"><strong>mclo.gs</strong></a> and provide the link. This site is specifically designed for Minecraft logs and offers helpful analysis.</li></ul><div class="tip custom-block"><p class="custom-block-title">Do Not Paste Directly</p><p>Please <strong>do not</strong> paste long logs or configuration files directly into Discord channels. Use the recommended services above and share the links.</p></div></li><li><strong>A Clear Description of the Issue:</strong><ul><li>What did you expect to happen?</li><li>What actually happened?</li><li>Were there any specific error messages shown to players or in the console that weren&#39;t in the full log?</li></ul></li><li><strong>Steps to Reproduce the Issue:</strong> If you can reliably make the issue happen, list the exact steps.</li></ul><p>The more detailed and accurately formatted information you provide using these services, the easier and faster it will be for the community or developers to assist you!</p><h3 id="f-server-appears-stuck-in-starting" tabindex="-1">F. Server appears stuck in &quot;starting&quot; <a class="header-anchor" href="#f-server-appears-stuck-in-starting" aria-label="Permalink to &quot;F. Server appears stuck in &quot;starting&quot;&quot;">​</a></h3><p>Symptoms:</p><ul><li>Players see messages that a server is starting, but it never seems to finish.</li><li>Subsequent attempts keep saying the server is starting.</li></ul><p>What VPP does automatically:</p><ul><li>VPP periodically runs a small cleanup sweep (about every 60 seconds) to verify the real status.</li><li>If the panel reports the server is online, VPP clears the &quot;starting&quot; state.</li><li>If the startup window has been exceeded, VPP also clears the stuck state so new attempts can proceed.</li></ul><p>What you can check:</p><ol><li>Enable debug logs (<code>loggerLevel: 10</code>) and watch for messages about the starting sweep.</li><li>Verify your <code>serverStatusCheckMethod</code> works in your environment (try switching between <code>VELOCITY_PING</code> and <code>PANEL_API</code> for diagnosis).</li><li>Ensure the backend server actually starts (check panel console logs for errors).</li><li>Confirm rate limiting isn’t blocking status checks (<code>printRateLimit: true</code>).</li><li>Try reconnecting after the sweep interval to see if the state was cleared. ere any specific error messages shown to players or in the console that weren&#39;t in the full log?</li></ol><ul><li><strong>Steps to Reproduce the Issue:</strong> If you can reliably make the issue happen, list the exact steps.</li></ul><p>The more detailed and accurately formatted information you provide using these services, the easier and faster it will be for the community or developers to assist you!</p>',59)]))}const p=o(r,[["render",l]]);export{h as __pageData,p as default};
